package com.company;
import java.util.Scanner;
public class negative {

    /*public static int quadEqn(float x, float y, float z)
    {
        float deter=(y*y)-(4*x*z);
       // float r1,r2;
        if(deter>0)
        {
            return 1;
        }
        else if(deter==0)
        {
            return 2;
        }
        else
        {
            return 3;
        }
    }
            public static void main (String[] args) {
                // Your code here
                float r1=0,r2=0,i,r;
                Scanner sc=new Scanner(System.in);
                float a=sc.nextFloat();
                float b=sc.nextFloat();
                float c=sc.nextFloat();

                float deter=(b*b)-(4*a*c);
                switch(quadEqn(a, b, c))
                {
                    case 1:
                        r1 = (float) ((-b + Math.sqrt(deter)) / (2 * a));
                        r2 = (float) ((-b - Math.sqrt(deter)) / (2 * a));
                        System.out.format(" %.2f\n",r1);
                        System.out.format("%.2f",r2);
                        break;
                    case 2:
                        r1 = (-b) / (2 * a);
                        r2 = (-b) / (2 * a);
                        System.out.format(" %.2f\n", r1);
                        System.out.format(" %.2f", r1);
                        break;
                    default:
                        r=(-b) / (2 * a);
                        i= (float) (Math.sqrt(-deter) / (2 * a));
                        System.out.format("%.2f+%.2fi\n", r, i);
                        System.out.format("%.2f-%.2fi", r, i);
                        break;
                }
            }
            }
     */


    }








